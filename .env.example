# Database
DATABASE_URL=postgres://dguesser:dguesser_dev@localhost:5433/dguesser

# Redis
REDIS_URL=redis://localhost:6381

# Server
API_HOST=0.0.0.0
API_PORT=3001
REALTIME_HOST=0.0.0.0
REALTIME_PORT=3002

# Auth
SESSION_SECRET=change-me-in-production-use-64-bytes-minimum
SESSION_TTL_HOURS=168

# OAuth - Google
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_REDIRECT_URI=http://localhost:3001/api/v1/auth/google/callback

# OAuth - Microsoft
MICROSOFT_CLIENT_ID=
MICROSOFT_CLIENT_SECRET=
MICROSOFT_REDIRECT_URI=http://localhost:3001/api/v1/auth/microsoft/callback

# Frontend URL (for CORS)
FRONTEND_URL=http://localhost:5173

# Logging
RUST_LOG=info,dguesser=debug,tower_http=debug

# ==============================================================================
# Location Provider Configuration
# ==============================================================================
# See docs/R2-CDN-SETUP.md for full setup guide

# Provider type: "postgres" (default) or "r2"
# - postgres: Use PostgreSQL locations table (existing system)
# - r2: Use R2/local pack files (new system for 100M+ locations)
# LOCATION_PROVIDER=postgres

# R2 Pack Provider Settings (only used when LOCATION_PROVIDER=r2)
# Base URL for R2 bucket or local file path:
# - R2: https://cdn.dguesser.lol (production)
# - Local: file:///path/to/packs (development)
# LOCATION_R2_URL=file://./data/packs

# Dataset version (directory name within the base URL)
# LOCATION_R2_VERSION=v2026-01

# Maximum disabled location hashes to cache in memory (default: 200000)
# LOCATION_MAX_DISABLED_CACHE=200000

# ==============================================================================
# R2 Upload Credentials (for rclone - NOT needed at runtime)
# ==============================================================================
# These are only used for uploading pack files via rclone.
# The application reads from the public URL, no credentials needed.
# Store these in ~/.config/rclone/rclone.conf instead of here.
#
# LOCATION_R2_ACCESS_KEY=your-r2-access-key-id
# LOCATION_R2_SECRET_ACCESS_KEY=your-r2-secret-access-key
# LOCATION_S3_API_URL=https://your-account-id.r2.cloudflarestorage.com/bucket-name
