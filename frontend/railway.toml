# Railway deployment configuration for the frontend service

[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"
buildContext = "."
watchPatterns = [
    "frontend/**",
]

[deploy]
healthcheckPath = "/"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
