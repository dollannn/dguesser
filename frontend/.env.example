# DGuesser Frontend Environment Variables
# Copy to .env for local development: cp .env.example .env

# =============================================================================
# BUILD-TIME VARIABLES (VITE_* prefix)
# These are baked into the JS bundle at build time.
# For production Docker builds, set these as build args in your CI/CD.
# =============================================================================

# API server URL (client-side requests)
VITE_API_URL=http://localhost:3001

# Realtime server URL (Socket.IO)
VITE_REALTIME_URL=http://localhost:3002

# Google Maps API Key (required for maps)
VITE_GOOGLE_MAPS_API_KEY=your_google_maps_api_key

# =============================================================================
# RUNTIME VARIABLES (no VITE_ prefix)
# These are read at runtime by the SvelteKit server.
# Set these as environment variables in your hosting platform.
# =============================================================================

# Server-side API URL for SSR requests
# Can be internal service URL (e.g., http://api:3001) or public URL
API_URL=http://localhost:3001

# Public site URL for canonical links and SEO
PUBLIC_SITE_URL=https://dguesser.lol
