# Railway deployment configuration
# This is the root configuration - each service should have its own railway.toml
# or use service-specific settings in the Railway dashboard.

[build]
builder = "dockerfile"

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
